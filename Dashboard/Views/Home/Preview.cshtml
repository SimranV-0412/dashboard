@model List<Dashboard.Models.ContentModel>
@{
    
    ViewData["Title"] = "Preview page";
}


<style>
    h1 {
        font-size: 30px;
        color: #fff;
        text-transform: uppercase;
        font-weight: 300;
        text-align: center;
        margin-bottom: 15px;
    }

    table {
        width: 100%;
        table-layout: fixed;
    }

    .tbl-header {
        background-color: rgba(255,255,255,0.3);
    }

    .tbl-content {
        height: 300px;
        overflow-x: auto;
        margin-top: 0px;
        border: 1px solid rgba(255,255,255,0.3);
    }

    th {
        padding: 20px 15px;
        text-align: left;
        font-weight: 500;
        font-size: 12px;
        color: #fff;
        text-transform: uppercase;
    }

    td {
        padding: 15px;
        text-align: left;
        vertical-align: middle;
        font-weight: 300;
        font-size: 12px;
        color: #fff;
        border-bottom: solid 1px rgba(255,255,255,0.1);
    }

    body {
        background: -webkit-linear-gradient(left, #25c481, #25b7c4);
        background: linear-gradient(to right, #25c481, #25b7c4);
        font-family: 'Roboto', sans-serif;
    }

    section {
        margin: 50px;
    }
    /* for custom scrollbar for webkit browser*/

    ::-webkit-scrollbar {
        width: 6px;
    }

    ::-webkit-scrollbar-track {
        -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
    }

    ::-webkit-scrollbar-thumb {
        -webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
    }
</style>
<div style="text-align: center">
    <h1> Content Detail </h1>
</div>

<section>
    <!--for demo wrap-->

    <div class="tbl-header">
        <table cellpadding="0" cellspacing="0" border="0">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Link</th>
                    <th>Edit</th>
                    <th>Delete</th>
                </tr>
            </thead>
        </table>
    </div>
    <div class="tbl-content">
        <table cellpadding="0" cellspacing="0" border="0">
            <tbody>

                @foreach (ContentModel item in Model)
                {
                    <tr>
                       @* <td>@item.Id</td>*@
                        <td>@item.Title</td>
                        <td>
                            <a href="data:text/html;charset=utf-8;base64,@ViewBag.EncodedHtmlContent" target="_blank">@ViewBag.HtmlFileName</a>
                        </td>
                       @* <td>@item.phone</td>*@
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>
<!-- Display the clickable link with the file name -->

@*<a href="data:text/html;charset=utf-8;base64,@ViewBag.EncodedHtmlContent" target="_blank">@ViewBag.HtmlFileName</a>
*@
@*<a id="htmlLink" href="#" target="_blank">@ViewBag.HtmlFileName</a>

<script>
    // Get the HTML link element
    var htmlLink = document.getElementById('htmlLink');

    // Attach a click event listener
    htmlLink.addEventListener('click', function (event) {
        event.preventDefault(); // Prevent the default link behavior

        // Get the encoded HTML content
        var encodedHtml = '@ViewBag.EncodedHtmlContent';

        // Create a new window or tab with the encoded HTML content
        var newWindow = window.open('', '_blank');
        newWindow.document.write(decodeURIComponent(escape(window.atob(encodedHtml))));
        newWindow.document.close();
    });
</script>*@